<div class='article'>
  <%= link_to channel_article_path(channel_article, :channel_article => {:starred => !channel_article.starred}), :method => :put do %>
    <span class="star star-<%= channel_article.starred? ? 'on' : 'off' %>"></span>
  <% end %>

  <%= render channel_article.article %>
</div>
